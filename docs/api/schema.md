# Schema

The `schema/` directory contains GraphQL schema files pulled directly from the upstream [stashapp/stash](https://github.com/stashapp/stash) repository for local reference.

These files define the official Stash GraphQL API schema and are used as the source of truth for type generation.
